<%= turbo_frame_tag note, class: 'item' do %>
  <section class="mt-3">
    <h2>
      <%= note.title %>
    </h2>

    <p class="lead">
      <%= note.description %>
    </p>

    <%= link_to 'Edit', edit_note_path(note), class: 'btn btn-secondary' %>
  </section>
<% end %>